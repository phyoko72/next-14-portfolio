import HeroSection from "@/ui/hero/HeroSection"
import Nav from "@/ui/hero/Nav"

export default function Hero() {
    return (
        <section id="Homepage">
            <Nav />
            <HeroSection />
        </section>
    )
}
